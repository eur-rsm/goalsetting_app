export enum StorageNames {
    config = 'config',
    configNeeded = 'configNeeded',
    userInput = 'userInput',
    buttons = 'buttons',
}

export enum EventNames {
    prepare = 'prepare',
    retrieve = 'retrieve',
    failure = 'failure',
}
